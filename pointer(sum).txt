#include <stdio.h>
void main()
{
    int a,b,sum;
    int *p,*q;
    printf("enter two numbers:");
    scanf("%d %d",&a,&b);
    p=&a;
    q=&b;
    sum=*p+*q;
    printf("sum=%d",sum);
    
}